const config = {
    db: {
        /* don't expose password or any sensitive info, done only for demo */
        host: "host",
        user: "user",
        password: "password",
        database: "database",
    },
    listPerPage: 10,
};
module.exports = config;